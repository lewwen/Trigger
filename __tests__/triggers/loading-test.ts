import { TriggerDefinitionNode } from '../../src/app/types';

describe('trigger loader', () => {
    describe('new', () => {
        it('initialize a new trigger', () => {
        });
    });
    
    describe('existing', () => {
        it('load a command with values selected in the parameters', () => {
        });
        
        it('load a command with inputted values in the parameters', () => {
        });

        it('handle non existant nodes', () => {
        });

        it('load a trigger after another already loaded', () => {
        });
        
        it('load a command with a command in a parameter', () => {
        });
    });
});